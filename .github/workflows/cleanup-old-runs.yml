name: Cleanup Old Workflow Runs

on:
  workflow_dispatch:

jobs:
  cleanup:
    runs-on: ubuntu-latest

    steps:
      - name: Cleanup Disabled
        run: |
          echo "================================"
          echo "Automatic cleanup is disabled."
          echo "No workflow runs will be deleted."
          echo "No artifacts will be deleted."
          echo "================================"
